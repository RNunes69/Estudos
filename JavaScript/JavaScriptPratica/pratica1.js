// Declare três variáveis diferentes (uma para cada tipo: string, número e booleano) e atribua valores a elas. Em seguida, exiba o tipo de cada variável no console.

/*
let string = 'Rafael';
var numero = 10;
const bool = true;

console.log('As variaveis sao:', string, numero, bool);
*/

//Crie duas variáveis, uma contendo seu primeiro nome e outra contendo seu último nome. Em seguida,combine-as em uma terceira variável
//usando o operador + e em uma quarta variável usando template strings.Por fim, imprima os resultados obtidos no console.

/*
const nome1 = 'Rafael';
const nome2 = 'Lima';

nome3 = nome1 + " " + nome2;

const nomec = `${nome1} ${nome2}`;

console.log(nome3);
console.log(nomec);
*/

//Declare duas variáveis, uma contendo um número e outra contendo uma string. Em seguida, combine-as em uma
//terceira variável usando template strings para montar uma frase e exiba o resultado no console.

/*
const num = 3;
const str = 'vidas';

comb = `Ele tem ${num} ${str}.`;
console.log(comb);
*/

//Crie uma variável inicializada com um valor de qualquer tipo e, em seguida, reatribua um novo valor a essa variável. Imprima ambos os valores no console após atribuí-los.

/*
var num = 3;
console.log(num);

num = 4;
console.log(num);
*/

//Declare uma variável booleana que informa se está chovendo e utilize-a em uma estrutura condicional (if) para exibir uma mensagem informando se é preciso levar um guarda-chuva ou não dependendo do valor da variável.

/*
const chuva = true

if (chuva) {
    console.log("Esta chovendo, leve um guarda chuva")
} else {
    console.log("Nao esta chovendo, nao precisa levar o guarda chuva")
}
*/